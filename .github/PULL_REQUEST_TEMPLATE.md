## What

## Why

## Checklist

- [ ] Rust only, no comments in code (docs go to the wiki)
- [ ] No new dependencies (or a very good reason below)
- [ ] `cargo fmt --check` + `cargo clippy --all-targets -- -D warnings` + `cargo test` pass
- [ ] New surfaces fail closed
